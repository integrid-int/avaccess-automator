services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: ha_panel_test
    restart: unless-stopped
    volumes:
      - ./config:/config
      # Track B/C: executor + Guide EPG builder; inventory / directv / guide_epg YAML
      - ../scripts/avaccess:/config/avaccess/scripts:ro
      - ../scripts/directv_shef.py:/config/avaccess/scripts/directv_shef.py:ro
      - ../config:/config/avaccess/config:ro
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=UTC
    ports:
      - "8123:8123"
